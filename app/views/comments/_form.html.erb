<form action ="<%= gossip_comments_path(gossip)%>" method="post">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <textarea name ="comment[content]" rows="8" cols="40"></textarea>
  <p><input type="submit" value="Ajouter un commentaire" class ="btn btn-warning"></p>
</form>

